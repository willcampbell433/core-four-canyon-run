{
  "name": "core-four-canyon-run",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node --check app.js && node --check shared-store.js && node --check scripts/generate-shared-config.mjs",
    "build": "npm test && npm run check && node scripts/generate-shared-config.mjs"
  }
}
